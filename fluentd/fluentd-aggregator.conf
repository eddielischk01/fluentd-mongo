<source>
  @type forward
  port 24224
</source>

# <filter fluentdbit.**>
#   @type parser
#   format json
#   key_name log
#   time_key time
#   time_format %Y-%m-%dT%H:%M:%S.%L%Z
# </filter>

# Single MongoDB
<match fluentdbit.**>
  @type copy
  <store>
    @type mongo

    # mongodb db + collection
    database audit
    collection access

    # mongodb host + port
    host mongo
    port 27017
    <buffer>
      flush_interval 1s
    </buffer>
  </store>
  <store>
    @type stdout
  </store>
 
</match>

